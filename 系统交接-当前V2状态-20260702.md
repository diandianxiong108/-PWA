## 任务管家 V2 当前系统交接

更新日期：2026-07-02

这份文档是给新的 AI / 新对话直接阅读的快速交接稿。目标是让接手方立刻理解：

1. 这个项目现在真正的主版本在哪里
2. 用户当前最在意什么
3. 哪些功能已经接上
4. 哪些问题还在修
5. 怎样继续改，才不会又回到旧版思路

---

## 1. 当前主版本判断

当前开发主版本是根目录这一套 V2 文件，不是旧版原始页面：

- `D:\库\桌面\任务管家PWA\index.html`
- `D:\库\桌面\任务管家PWA\v2.js`
- `D:\库\桌面\任务管家PWA\v2.css`
- `D:\库\桌面\任务管家PWA\ai-memory.js`
- `D:\库\桌面\任务管家PWA\sw.js`
- `D:\库\桌面\任务管家PWA\assets\v2-stickers\`

用于云构建 / GitHub Actions / APK 打包的镜像目录是：

- `D:\库\桌面\任务管家PWA\cloud-build-repo\`

注意：

- 根目录不是 Git 仓库
- `cloud-build-repo` 才是可 push 的仓库
- 真正要发到云端、触发 APK 构建，必须把根目录当前版本同步进 `cloud-build-repo`

---

## 2. 用户要的不是普通待办，而是低阻力任务管家

用户对产品的核心要求不是“再做一个待办软件”，而是：

- 少输入
- 低阻力
- 能理解自然语言
- 能识别状态、能量、隐形成本、账单、喝水、明日建议
- 能把任务按节奏安排，不要硬塞
- AI 要逐渐记住用户习惯
- 页面要可爱，但不能华而不实

一句话总结：

这是一个“会陪着用户生活和做事”的个人任务管家，不是后台管理系统。

---

## 3. 当前视觉方向

当前已确定的视觉方向是：

### 方案 B / 贴纸手帐风

关键词：

- 奶油纸张底
- 便签卡片
- 胶带装饰
- 小猫贴纸
- 彩色贴纸图标
- 温暖、可爱、清晰

已经做过的资源体系：

- `assets/v2-stickers/cats/`
- `assets/v2-stickers/icons/`
- `assets/v2-stickers/tapes/`
- `assets/v2-stickers/decorations/`
- 若干 `detail-icons-v2/`、`icons-collage-v2/`、`ui-icons-collage-v1/` 贴纸图标资源

### 视觉硬规则

后续任何页面都不要再回到：

- 普通后台表单风
- 大块空白
- 裸系统 emoji
- 纯白空卡片
- 竖排文字
- 装饰挡字

---

## 4. 当前首页状态

HomeV2 已经不是最初的旧首页了，方向已切到贴纸手帐风。

首页现在的目标结构是：

1. 顶部欢迎区
2. 今日进度 Hero
3. 今天计划
4. 快捷入口 2x4
5. 今日小记
6. 底部导航

另外：

- AI 助手和智能识别应保持独立入口
- 底部导航要固定在所有页面
- “规划”页需要承接周 / 月 / 年 / 时段视图

---

## 5. 二级页面当前状态

已经开始统一到 V2 风格的页面包括：

- 每日任务
- 项目推进
- 循环琐事
- 临时任务
- 任务池
- 生活能量与节奏
- 居家模式 / 当日详情
- 智能规划室

但要注意：

### 当前二级页仍有未完全收口的问题

1. 个别 Hero 右侧布局容易错位
2. 某些页面仍可能出现内容重叠
3. 本地入口与云端入口曾混用，导致用户看见“没更新”
4. 非任务型页面（账单、额外完成、成就勋章、灵感便签、随手记）还需要按各自功能单独排版，不能继续硬套“任务完成度 Hero”

### 一个重要规则

不是所有二级页面都要显示：

- 0% 完成度
- 待完成数
- 任务进度条

只有任务类、节奏类页面才适合用这一套。

像下面这些页面必须单独做版式：

- 账单
- 额外完成
- 成就勋章
- 灵感便签
- 随手记
- 云备份 / 设置

---

## 6. AI 功能要求（必须牢记）

用户对 AI 的要求非常具体，不能再退回“普通聊天框”。

### 6.1 AI 整理 / 智能识别

用户输入一大段自然语言后，系统应先识别内容类型，再给结构化预览，再允许一键上传。

至少要识别：

- 已完成事项
- 待办事项
- 状态记录
- 心情记录
- 打断源
- 隐形成本
- 账单
- 喝水记录
- 明日调整建议

上传后必须明确告诉用户：

- 哪一类数据写到了哪里
- 成功几条
- 失败几条
- 失败原因是什么

不能只说“上传成功”或“遇到一点麻烦”。

### 6.2 AI 助手

AI 助手不是纯识别器，要保留陪伴和规划属性。

用户明确要求过的模式包括：

- 陪我聊
- 轻量整理
- 安排今天
- 拆大项目
- 查看记忆

### 6.3 AI 记忆

用户非常在意 AI 是否“记得她”。

记忆策略要求：

- 不必保存每句原始聊天
- 但必须定期提炼长期偏好和习惯
- 下次对话回答时要参考这些长期摘要
- 既要省 token，也要保留人格理解

当前项目里已接：

- `ai-memory.js`
- 周摘要
- 用户习惯提取
- 对话记忆入口

但后续仍要检查：

- AI 助手页面里这些入口是否清楚可见
- 回答时是否真的用了记忆
- 是否能对任务池调度起作用

### 6.4 任务池智能调度

这是用户很看重的能力：

- 用户问“今天还有什么可以做”
- AI 需要结合：
  - 今日状态
  - 今日已安排多少
  - 任务池待安排内容
- 然后给 3 个左右可选项

这个能力不能丢。

---

## 7. 用户新增关注记录模块

用户明确要求新增并接入 AI 复盘：

1. 饮食波动 / 暴饮暴食记录
2. 熬夜记录

原因：

- 用户回家、压力大时容易暴饮暴食
- 容易熬夜到凌晨 3-4 点
- 想结合当日心情和经过，让 AI 帮忙复盘原因

建议放在：

- 生活能量与节奏

同时：

- 这两类数据后续应进入 AI 复盘上下文

---

## 8. 用户明确讨厌的东西

接手时务必避免：

1. 竖排文字
2. 装饰挡住标题 / 进度 / 数字
3. 页面边距不对称
4. 小猫压住内容
5. 看起来像旧版只加了一点贴纸
6. 用系统默认图标或 emoji 糊弄
7. 任务页面所有按钮摊开像后台系统
8. 用户点进去是空白页
9. 用户刷新后看到的不是当前版本

---

## 9. 当前最需要继续收口的技术问题

### 9.1 本地入口 / 云端入口混淆

用户之前看到页面没变化，核心原因之一是：

- 有时打开本地 `index.html#/daily`
- 有时打开云端 Netlify 根路径
- 有时尝试 `/v2/` 但线上并未发布该路径

所以接手者要先确认：

- 当前用户看的到底是本地入口还是线上入口
- 当前线上根目录是不是已经同步了 V2

### 9.2 云端 V2 路径未真正独立发布

已确认：

- `https://soft-douhua-52d678.netlify.app/v2/` 目前会 404

这说明：

- 线上不是把 `v2/` 子目录单独作为发布目录
- 需要重新整理发布结构，避免再给用户错误入口

### 9.3 `cloud-build-repo` 与根目录代码未必一致

因为当前主要修改一直发生在根目录：

- `index.html`
- `v2.js`
- `v2.css`
- `ai-memory.js`
- `sw.js`
- `assets/`

而云构建依赖：

- `cloud-build-repo/`

所以发布前必须同步。

---

## 10. APK / 云构建流程

当前 APK 构建仓库在：

- `D:\库\桌面\任务管家PWA\cloud-build-repo`

GitHub Actions 工作流：

- `cloud-build-repo/.github/workflows/build-apk.yml`

工作流会：

1. checkout
2. 安装 Node / Java / Android SDK
3. `npm run cap:sync`
4. 恢复签名证书
5. `assembleRelease`
6. 发布 GitHub Release
7. 上传 APK Artifact

因此发布步骤应该是：

1. 把根目录当前 V2 代码同步到 `cloud-build-repo`
2. 提交到 `cloud-build-repo` 仓库
3. push 到 GitHub `main`
4. 等 GitHub Actions 出包
5. 把 APK 下载地址给用户

---

## 11. 推荐接手顺序

如果是新的 AI 继续做，推荐按这个顺序：

1. 先确认当前用户看的入口
2. 先确认根目录和 `cloud-build-repo` 哪个是最新
3. 先同步当前 V2 到 `cloud-build-repo`
4. 生成并更新 APK
5. 再继续做二级页面的个别错位修复
6. 再补账单 / 灵感便签 / 随手记 / 成就勋章等非任务页的专属版式
7. 再把饮食波动 / 熬夜记录正式接进生活能量和 AI 复盘

---

## 12. 给接手 AI 的一句提醒

这个用户不缺创意，也不缺产品感觉。她最烦的是：

- 明明说过很多次的问题还在反复出现
- 做出来的页面看起来和之前没区别
- 入口错了、版本错了、刷新没变化

所以接手时最重要的是：

先对齐“她现在实际看到的是哪一版”，再动手。

